<resources>

    <!-- Tips theme -->
    <style name="TipsTheme" parent="Theme.AppCompat.NoActionBar" />

    <!-- Splash theme -->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar" />

    <!-- Location application theme. -->
    <style name="LocationTheme" parent="Theme.AppCompat.Light.NoActionBar">

    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="LivePreviewTheme" parent="Theme.AppCompat.NoActionBar" >
    <!-- Customize your theme here. -->

    </style>

</resources>
